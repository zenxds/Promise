{
  "name": "Promise",
  "version": "0.1.0",
  "description": "A Promise/A+ implement",
  "scripts": {
    "test": "node_modules/.bin/mocha --recursive --debug --bail --timeout 30000",
    "cover": "node_modules/.bin/istanbul cover node_modules/.bin/_mocha test/*.js"
  },
  "devDependencies": {
    "expect.js": "~0.3.1",
    "istanbul": "~0.4.2",
    "mocha": "~2.4.5",
    "mocha-istanbul": "~0.2.0"
  }
}
