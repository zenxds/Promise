{
  "name": "Promise",
  "version": "0.1.0",
  "description": "A Promise/A+ implement",
  "scripts": {
    "test": "nyc mocha --recursive --bail --timeout 30000"
  },
  "devDependencies": {
    "expect.js": "^0.3.1",
    "mocha": "^5.0.0",
    "nyc": "^12.0.2"
  }
}
